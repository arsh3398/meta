import { SlineInDirective } from './sline-in.directive';

describe('SlineInDirective', () => {
  it('should create an instance', () => {
    const directive = new SlineInDirective();
    expect(directive).toBeTruthy();
  });
});
